#task_1.h

class Matrix:

    Constructor(std::vector<std::vector<double>>) creates a matrix. Class keeps only vector.

    void LineMultiply(std::vector<std::vector<double>>& vector, double multiply_number, int the_line_num) multiplies
    the_line_number by multiply_number.


std::vector<std::vector<double>>* Task_1(std::vector<std::vector<double>>& original_matrix). At first get the biggest of
elements [n - 1][n] and [n][n].